import { SCREEN_MARGIN } from "constants/app";

export const WIDTH = 400;
export const HEIGHT = 56;

export const POSITION_OPENED = new UDim2(0.5, 0, 1, 0);
export const POSITION_CLOSED = new UDim2(0.5, 0, 1, HEIGHT + SCREEN_MARGIN * 2);
