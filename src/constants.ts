export const IS_DEV = readfile === undefined;
export const VERSION_TAG = VERSION ?? "studio";

// Folder containing saved Rodux state
export const ORCA_PATH = "_orca";

// Above topbar, below prompts
export const ORCA_DISPLAY_ORDER = 7;
